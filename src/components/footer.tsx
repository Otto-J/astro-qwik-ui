import { component$ } from "@builder.io/qwik";

export default component$(() => {
  // footer
  return (
    <footer class="my-4 text-center bg-stone-400">
      <div class="container mx-auto ">footer</div>
    </footer>
  );
});
